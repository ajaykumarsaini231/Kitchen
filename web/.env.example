# Base URL of the API server (used server-side for ISR fetch and client-side).
NEXT_PUBLIC_API_URL=http://localhost:4000
# Socket.IO server URL for real-time menu updates.
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
# Public site URL (used for canonical/SEO/sitemap).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
